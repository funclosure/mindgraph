node_modules/
.DS_Store

# Playwright artifacts (screenshots, traces)
.playwright-mcp/
*.png
# Curated screenshots live in docs/screenshots/ and are tracked.
!docs/screenshots/*.png

# Superpowers brainstorm session artifacts
.superpowers/

# Local git worktrees
.worktrees/

# Smoke-test scratch output (regenerated by `npm run test:smoke`; the canonical
# sample the UI/tests rely on — episode-1-built — stays tracked).
examples/out/awakening.mindgraph.json
examples/out/empty.mindgraph.json

# Local produced graph documents / experiments
graphs/

# Local UI feedback packets
.mindgraph-feedback/

# Local source transcripts / content workspace inputs
transcripts/
